package practice.basicfeature;

import java.util.*;
import java.util.stream.Collectors;

import static java.util.Calendar.*;

public class Main {
    public static void main(String[] args) {
        DataStructure d = new DataStructure(10);
        d.id = 100;

        System.out.printf("ID:%d\n", d.id);
    }
}